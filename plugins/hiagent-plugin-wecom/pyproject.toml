[tool.poetry]
authors = []
name = "hiagent-plugin-wecom"
version = "0.2.1"
description = ""
package-mode = true
include = [
  "hiagent_plugin_wecom/icon.png",
  "hiagent_plugin_wecom/*_metadata.yaml",
]

[tool.poetry.scripts]
gen_metadata = 'hiagent_plugin_wecom:WecomPlugin._gen_metadata'

[tool.poetry.dependencies]
python = "^3.11"
httpx = "^0.28.1"
pydantic = "^2.10.2"

[tool.poetry.group.dev.dependencies]
hiagent_plugin_sdk = { path = "../../hiagent-plugin-sdk" }

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.plugins."hiagent.plugins"]
"企业微信" = "hiagent_plugin_wecom:WecomPlugin"
