[tool.poetry]
authors = []
name = "hiagent-plugin-wolfram-alpha"
version = "1.1.0"
description = ""
package-mode = true
include = [
  "hiagent_plugin_wolfram_alpha/icon.png",
  "hiagent_plugin_wolfram_alpha/*_metadata.yaml",
]

[tool.poetry.scripts]
gen_metadata = 'hiagent_plugin_wolfram_alpha:WolframAlphaPlugin._gen_metadata'

[tool.poetry.dependencies]
python = "^3.11"
langchain-community = "^0.3.4"
# hiagent_plugin_sdk = { path = "../../hiagent-plugin-sdk" }
wolframalpha = "^5.1.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.plugins."hiagent.plugins"]
WolframAlpha = "hiagent_plugin_wolfram_alpha:WolframAlphaPlugin"
